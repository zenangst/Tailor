osx_image: xcode8
language: objective-c

before_install:
- brew update
- if brew outdated | grep -qx xctool; then brew upgrade xctool; fi
- travis_wait 25 pod repo update --silent
- pod install

script:
- xcodebuild clean build -workspace Tailor.xcworkspace -scheme Tailor-iOS -sdk iphonesimulator
- xcodebuild test -workspace Tailor.xcworkspace -scheme Tailor-iOS -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6,OS=10.0'
- xcodebuild clean build -workspace Tailor.xcworkspace -scheme Tailor-Mac -sdk macosx
- xcodebuild test -workspace Tailor.xcworkspace -scheme Tailor-Mac -sdk macosx

notifications:
  email: false
